{
  "name": "copyboard-lite-web",
  "private": true,
  "version": "0.1.0",
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "check": "calcit --compat-types --check-only",
    "compile": "calcit --compat-types js",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build"
  },
  "devDependencies": {
    "@calcit/procs": "0.17.0",
    "vite": "6.1.0"
  }
}
